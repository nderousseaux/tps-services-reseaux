Vérifier la syntaxe du name.conf
```shell
named-checkconf
```

Vérifier la syntaxe des fichiers de zones
```shell
named-checkzone
```

#Client

Interroger le serveur dns
```shell
nslookup <adresse-serveur>
```

#Serveur
Lancer le serveur
```shell
named -g
```